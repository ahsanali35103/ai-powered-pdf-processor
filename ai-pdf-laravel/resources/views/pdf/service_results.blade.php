<!DOCTYPE html>
<html>
<head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8"/>
    <title>Service Results PDF</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 20px;
        }
        h1 {
            color: #333;
            border-bottom: 2px solid #ccc;
            padding-bottom: 5px;
        }
        .service-block {
            margin-bottom: 30px;
            padding: 15px;
            border: 1px solid #ddd;
            background-color: #f9f9f9;
        }
        .service-title {
            font-size: 18px;
            font-weight: bold;
            margin-bottom: 10px;
            text-transform: capitalize;
            color: #444;
        }
        .service-meta {
            font-size: 12px;
            color: #777;
            margin-bottom: 15px;
        }
        .service-content {
            font-size: 14px;
            line-height: 1.6;
            white-space: pre-wrap;
            word-wrap: break-word;
        }
        .footer {
            margin-top: 50px;
            font-size: 12px;
            text-align: center;
            color: #999;
        }
    </style>
</head>
<body>
    <h1>Processing Results</h1>
    <p><strong>File Name:</strong> {{ $fileName ?? 'Unknown' }}</p>
    <p><strong>Generated At:</strong> {{ \Carbon\Carbon::now()->toDateTimeString() }}</p>

    <hr>

    @if($services && count($services) > 0)
        @foreach($services as $service)
            <div class="service-block">
                <div class="service-title">
                    {{ $service->type }} Service
                    @if($service->type === 'translation' && $service->target_language)
                        ({{ $service->target_language }})
                    @endif
                </div>
                <div class="service-meta">
                    Status: {{ ucfirst($service->status) }} | 
                    Completed: {{ $service->completed_at ? $service->completed_at->toDateTimeString() : 'Pending' }}
                </div>
                
                <div class="service-content">
                    @if($service->status === 'completed' && $service->output)
                        @if($service->type === 'ocr')
                            {{ $service->output['text'] ?? 'No text output.' }}
                        @elseif($service->type === 'summarization')
                            {{ $service->output['summary'] ?? 'No summary output.' }}
                        @elseif($service->type === 'translation')
                            {{ $service->output['translated_text'] ?? 'No translation output.' }}
                        @else
                            {{ json_encode($service->output, JSON_PRETTY_PRINT) }}
                        @endif
                    @else
                        <em>Processing is not complete or no output is available.</em>
                    @endif
                </div>
            </div>
        @endforeach
    @else
        <p>No service results found.</p>
    @endif

    <div class="footer">
        Generated by AI-Powered PDF Processor
    </div>
</body>
</html>
